  const randomIndex = Math.floor(Math.random() * quiz.length); // hier wurde einen random Index generiert
  const randomQuestion = quiz.splice(randomIndex, 1)[0]; // splice gibt eine Array zurück [0] greift auf das erste element und das einzigste element
  return randomQuestion;